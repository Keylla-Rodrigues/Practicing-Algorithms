Algoritmo "Operador_logico"

Var
   A, B, C: Inteiro

Inicio
      A <- 2
      B <- 3
      C <- 5

      Escreval (NAO((A=B) OU (C>A)))
      //A n eh = B -> falso. C eh maior q A -> verdadeiero.
      //Um ou outro -> verdadeiro. Mas com o nao na frente ...dara falso.

      Fimalgoritmo